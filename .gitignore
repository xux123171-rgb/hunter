# 工具二进制（~264MB）不入库，靠 tools/install-toolchain.sh 重建
bin/*.exe

# 工作产物 / 归档（每个目标打完即删或单独归档，不随项目走）
scratch/
journal/
reports/
*.tmp

# 系统
Thumbs.db
Desktop.ini
.DS_Store
__pycache__/
*.pyc

# GitHub 推送凭证（只放本地，绝不入库/进聊天）
.hunter-token
